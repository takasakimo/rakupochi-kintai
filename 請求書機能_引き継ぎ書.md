# 請求書機能 実装引き継ぎ書

## 📋 実装状況サマリー

**現在の進捗**: フェーズ10完了、全フェーズ実装完了  
**完了率**: 100%（10/10フェーズ完了）🎉

## ✅ 完了済みフェーズ

### フェーズ1: データベーススキーマ設計・実装 ✅
- [x] Prismaスキーマに請求先企業マスタテーブルを追加
- [x] Prismaスキーマに請求書テーブルを追加
- [x] Prismaスキーマに請求明細テーブルを追加
- [x] Employeeテーブルに請求関連フィールドを追加
- [x] マイグレーション実行（`migration_add_invoice_tables.sql`）
- [x] Prisma Client再生成

**実装ファイル**:
- `prisma/schema.prisma` - BillingClient, Invoice, InvoiceDetailモデル追加
- `migration_add_invoice_tables.sql` - データベースマイグレーションSQL

### フェーズ2: 請求先企業マスタ管理機能 ✅
- [x] 請求先企業マスタ一覧ページ作成 (`/app/admin/billing-clients/page.tsx`)
- [x] 請求先企業マスタCRUD API作成 (`/app/api/admin/billing-clients/route.ts`)
- [x] サイドバーに「請求先企業管理」メニュー追加

**実装ファイル**:
- `app/admin/billing-clients/page.tsx` - 請求先企業マスタ管理ページ
- `app/api/admin/billing-clients/route.ts` - CRUD API
- `app/api/admin/billing-clients/[id]/route.ts` - 個別操作API
- `components/Sidebar.tsx` - メニュー追加

### フェーズ3: 従業員管理への請求情報追加 ✅
- [x] 従業員管理ページに請求情報セクション追加
- [x] 請求先企業プルダウン実装
- [x] 請求単価、残業設定、稼働日数入力欄追加
- [x] 従業員更新APIに請求情報フィールド追加
- [x] 従業員一覧に請求先企業情報表示

**実装ファイル**:
- `app/admin/employees/page.tsx` - 請求情報セクション追加
- `app/api/admin/employees/[id]/route.ts` - 請求情報フィールド追加
- `app/api/admin/employees/route.ts` - 請求情報を含む取得API

### 追加実装: 請求書作成機能のON/OFF設定 ✅
- [x] Prismaスキーマに`enableInvoice`フィールド追加
- [x] 企業選択画面の設定モーダルにチェックボックス追加
- [x] 企業管理ページの設定モーダルにチェックボックス追加
- [x] APIで`enableInvoice`設定の取得・更新に対応
- [x] マイグレーションSQL作成（`migration_add_enable_invoice.sql`）

**実装ファイル**:
- `prisma/schema.prisma` - `enableInvoice`フィールド追加
- `app/super-admin/select-company/page.tsx` - 設定モーダル追加
- `app/super-admin/companies/page.tsx` - 設定モーダル追加
- `app/api/super-admin/companies/[id]/settings/route.ts` - API更新
- `migration_add_enable_invoice.sql` - マイグレーションSQL

### フェーズ4: 請求書管理機能（基本） ✅
- [x] 請求書一覧ページ作成
- [x] 請求書CRUD API作成
- [x] 請求書個別操作API作成
- [x] サイドバーに「請求書管理」メニュー追加（`enableInvoice`がtrueの場合のみ表示）

**実装ファイル**:
- `app/admin/invoices/page.tsx` - 請求書一覧ページ（ステータス・請求先企業フィルタリング対応）
- `app/api/admin/invoices/route.ts` - 請求書一覧取得・作成API
- `app/api/admin/invoices/[id]/route.ts` - 請求書取得・更新・削除API
- `components/Sidebar.tsx` - 請求書管理メニュー追加（`enableInvoice`設定に基づく条件表示）

## 📝 次に実装すべきフェーズ

### フェーズ8: 請求書PDF生成機能
**優先度**: 高

- [x] 4.1 請求書一覧ページ作成 (`/app/admin/invoices/page.tsx`)
- [x] 4.2 請求書作成API作成 (`/app/api/admin/invoices/route.ts`)
- [x] 4.3 請求書取得API作成
- [x] 4.4 請求書更新API作成 (`/app/api/admin/invoices/[id]/route.ts`)
- [x] 4.5 請求書削除API作成
- [x] 4.6 請求書一覧表示実装
- [x] 4.7 サイドバーに「請求書管理」メニュー追加（`enableInvoice`がtrueの場合のみ表示）

**実装ファイル**:
- `app/admin/invoices/page.tsx` - 請求書一覧ページ
- `app/api/admin/invoices/route.ts` - 請求書CRUD API
- `app/api/admin/invoices/[id]/route.ts` - 請求書個別操作API
- `components/Sidebar.tsx` - 請求書管理メニュー追加

**実装のポイント**:
- サイドバーの表示制御は`enableInvoice`設定を確認してから表示
- 請求書一覧はステータス（下書き、発行済み、請求済み、支払済み）でフィルタリング可能に
- 請求期間、請求先企業で検索・フィルタリング機能を実装

### フェーズ5: 請求書作成フォーム ✅
**優先度**: 高

- [x] 5.1 請求書作成ページ作成 (`/app/admin/invoices/new/page.tsx`)
- [x] 5.2 請求先企業選択プルダウン実装
- [x] 5.3 請求期間選択実装
- [x] 5.4 請求書番号自動採番機能実装
- [x] 5.5 対象従業員選択機能実装（チェックボックス）
- [x] 5.6 請求書基本情報入力フォーム実装

**実装ファイル**:
- `app/admin/invoices/new/page.tsx` - 請求書作成ページ
- `app/api/admin/invoices/generate-number/route.ts` - 請求書番号自動生成API

**実装のポイント**:
- 請求先企業の`invoiceNumberPrefix`を使用して請求書番号を自動生成（例: `INV-2026-001`）
- 請求期間はHTML5の`type="date"`で選択可能に
- 対象従業員は請求先企業に紐づいている従業員のみ表示（チェックボックスで複数選択可能）
- 請求期間終了日から自動的にお支払い期日を計算（月末締め翌月末払い）

### フェーズ6: 勤怠データからの自動計算機能 ✅
**優先度**: 高

- [x] 6.1 請求期間の勤怠データ取得API作成
- [x] 6.2 勤務日数計算ロジック実装
- [x] 6.3 基本金額計算ロジック実装
- [x] 6.4 残業時間計算ロジック実装（残業の有無がONの場合）
- [x] 6.5 残業金額計算ロジック実装
- [x] 6.6 欠勤判定・減算計算ロジック実装
- [x] 6.7 遅刻・早退判定・減算計算ロジック実装
- [x] 6.8 明細自動生成機能実装
- [x] 6.9 合計金額自動計算機能実装

**実装ファイル**:
- `app/api/admin/invoices/calculate-details/route.ts` - 請求書明細自動計算API
- `app/admin/invoices/new/page.tsx` - 請求書作成ページ（自動計算機能統合）

**実装のポイント**:
- 従業員の`billingRate`、`overtimeRate`、`hasOvertime`を使用
- シフトデータと打刻データを組み合わせて計算
- 請求先企業の`taxRate`を使用して消費税を計算
- 企業設定の`allowPreOvertime`、`standardBreakMinutes`を考慮
- 遅刻・早退は15分以上で減算（1回あたり1000円）
- 欠勤は基本単価を減算

### フェーズ7: 請求書編集機能 ✅
**優先度**: 中

- [x] 7.1 請求書編集ページ作成 (`/app/admin/invoices/[id]/edit/page.tsx`)
- [x] 7.2 明細の手動編集機能実装
- [x] 7.3 交通費入力機能実装
- [x] 7.4 調整金額入力機能実装
- [x] 7.5 消費税計算機能実装
- [x] 7.6 請求書保存機能実装（下書き状態）

**実装ファイル**:
- `app/admin/invoices/[id]/edit/page.tsx` - 請求書編集ページ
- `app/api/admin/invoices/[id]/route.ts` - 明細更新機能追加

**実装のポイント**:
- 請求書データを取得して編集可能なフォームに表示
- 明細の各項目（勤務日数、基本単価、基本金額、残業時間、残業金額、欠勤日数、欠勤減算、遅刻・早退減算）を手動編集可能
- 交通費と調整金額を入力可能
- 消費税を自動計算（請求先企業の税率を使用）
- 保存時に明細も含めて更新（既存明細を削除して新規作成）

### フェーズ8: 請求書PDF生成機能 ✅
**優先度**: 中

- [x] 8.1 PDF生成ライブラリ確認（jspdf + jspdf-autotable 既に導入済み）
- [x] 8.2 請求書PDFテンプレート作成
- [x] 8.3 PDF生成API作成 (`/app/api/admin/invoices/[id]/pdf/route.ts`)
- [x] 8.4 請求書発行機能実装（ステータス更新 + PDF生成）
- [x] 8.5 PDFダウンロード機能実装

**実装ファイル**:
- `app/api/admin/invoices/[id]/pdf/route.ts` - PDF生成API
- `app/admin/invoices/[id]/edit/page.tsx` - PDFダウンロード・発行機能追加

**実装のポイント**:
- jspdfとjspdf-autotableを使用してPDFを生成
- 請求元企業・請求先企業の情報を表示
- 請求明細をテーブル形式で表示
- 交通費・調整金額も明細に含める
- 合計金額（税抜・税込）を表示
- 請求書編集ページからPDFダウンロード可能
- 請求書発行機能：ステータスを「発行済み」に更新し、自動的にPDFをダウンロード

### フェーズ9: UI/UX改善 ✅
**優先度**: 低

- [x] 9.1 請求書一覧のフィルタリング機能（既に実装済み）
- [x] 9.2 請求書一覧の検索機能
- [x] 9.3 請求書コピー機能実装
- [x] 9.4 バリデーション実装
- [x] 9.5 エラーハンドリング実装
- [x] 9.6 ローディング状態の表示（既に実装済み）

**実装ファイル**:
- `app/admin/invoices/page.tsx` - 検索機能・コピー機能追加
- `app/admin/invoices/new/page.tsx` - バリデーション・エラーハンドリング強化
- `app/admin/invoices/[id]/edit/page.tsx` - バリデーション・エラーハンドリング強化

**実装のポイント**:
- 検索機能：請求書番号、件名、請求先企業名でリアルタイム検索
- コピー機能：既存の請求書をコピーして新しい請求書を作成（件名に「（コピー）」を追加）
- バリデーション：日付の妥当性チェック、明細の値チェック、交通費の妥当性チェック
- エラーハンドリング：詳細なエラーメッセージ表示、ネットワークエラーの適切な処理

### フェーズ10: テスト・デプロイ ✅
**優先度**: 低

- [x] 10.1 動作確認チェックリスト作成
- [x] 10.2 本番環境へのデプロイ完了
- [x] 10.3 ビルドエラー修正完了

**実装ファイル**:
- `請求書機能_動作確認チェックリスト.md` - 動作確認チェックリスト

**実装のポイント**:
- 動作確認チェックリストを作成し、全機能の確認項目を整理
- 本番環境へのデプロイが完了
- ビルドエラー（PDF生成APIのtaxRate取得）を修正

**注意事項**:
- 単体テスト・統合テストは未実装（プロジェクトにテストフレームワークが導入されていないため）
- 動作確認は`請求書機能_動作確認チェックリスト.md`を使用して実施してください

## 📊 データベーススキーマ

### BillingClient (請求先企業マスタ)
```prisma
model BillingClient {
  id              Int       @id @default(autoincrement())
  companyId       Int       // 請求元企業
  name            String    // 請求先企業名
  code            String?   // 請求先企業コード
  address         String?   // 住所
  phone           String?   // 電話番号
  fax             String?   // FAX番号
  contactPerson   String?   // 担当者名
  bankName        String?   // 銀行名
  bankBranch      String?   // 支店名
  accountNumber   String?   // 口座番号
  accountHolder   String?   // 口座名義
  taxRate         Float     @default(0.1) // 消費税率（10% = 0.1）
  invoiceNumberPrefix String? // 請求書番号プレフィックス
  isActive        Boolean   @default(true)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  company         Company   @relation(fields: [companyId], references: [id], onDelete: Cascade)
  employees       Employee[]
  invoices        Invoice[]
  
  @@map("billing_clients")
}
```

### Invoice (請求書)
```prisma
model Invoice {
  id              Int       @id @default(autoincrement())
  companyId       Int       // 請求元企業
  billingClientId Int       // 請求先企業
  invoiceNumber   String    @unique // 請求書番号
  subject         String    // 件名
  periodStart     DateTime  @db.Date // 請求期間開始
  periodEnd       DateTime  @db.Date // 請求期間終了
  paymentTerms    String    // 代金決済条件
  dueDate         DateTime  @db.Date // お支払い期日
  subtotal        Int       // 小計（税抜）
  taxAmount       Int       // 消費税額
  totalAmount     Int       // 合計金額（税込）
  transportationCost Int?   @default(0) // 交通費
  adjustmentAmount Int?     @default(0) // 調整金額
  status          String    @default("draft") // draft, issued, billed, paid
  issuedAt        DateTime? // 発行日
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  company         Company   @relation(fields: [companyId], references: [id], onDelete: Cascade)
  billingClient   BillingClient @relation(fields: [billingClientId], references: [id], onDelete: Cascade)
  details         InvoiceDetail[]
  
  @@map("invoices")
}
```

### InvoiceDetail (請求明細)
```prisma
model InvoiceDetail {
  id              Int       @id @default(autoincrement())
  invoiceId       Int
  employeeId      Int
  workDays        Int       // 勤務日数
  basicRate       Int       // 基本単価
  basicAmount     Int       // 基本金額
  overtimeHours   Float?    @default(0) // 残業時間
  overtimeRate    Float?    // 残業単価
  overtimeAmount  Int?      @default(0) // 残業金額
  absenceDays     Int?      @default(0) // 欠勤日数
  absenceDeduction Int?     @default(0) // 欠勤減算額
  lateEarlyDeduction Int?   @default(0) // 遅刻・早退減算額
  subtotal        Int       // 小計
  
  invoice         Invoice   @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  employee        Employee  @relation(fields: [employeeId], references: [id])
  
  @@map("invoice_details")
}
```

### Employee テーブルへの追加フィールド
```prisma
model Employee {
  // ... 既存フィールド
  billingClientId Int?      // 請求先企業ID（追加）
  billingRate     Int?      // 請求単価（時給または日給）
  overtimeRate    Float?    @default(1.25) // 残業単価倍率
  hasOvertime     Boolean   @default(false) // 残業の有無
  baseWorkDays    Int?      @default(22) // 稼働日数のベース（月間）
  
  billingClient   BillingClient? @relation(fields: [billingClientId], references: [id])
}
```

### CompanySetting テーブルへの追加フィールド
```prisma
model CompanySetting {
  // ... 既存フィールド
  enableInvoice   Boolean   @default(false) // 請求書作成機能を有効にするかどうか
}
```

## 🔧 技術スタック

- **フロントエンド**: Next.js 14.2.33, React, TypeScript
- **バックエンド**: Next.js API Routes
- **データベース**: PostgreSQL (Supabase)
- **ORM**: Prisma 5.22.0
- **認証**: NextAuth.js

## 📁 重要なファイルパス

### 実装済み
- `app/admin/billing-clients/page.tsx` - 請求先企業マスタ管理
- `app/api/admin/billing-clients/route.ts` - 請求先企業マスタAPI
- `app/admin/employees/page.tsx` - 従業員管理（請求情報セクション追加済み）
- `app/super-admin/select-company/page.tsx` - 企業選択画面（設定モーダル追加済み）
- `migration_add_invoice_tables.sql` - 請求書テーブル作成マイグレーション
- `migration_add_enable_invoice.sql` - enableInvoiceフィールド追加マイグレーション

### 次に実装すべき機能
- 請求書一覧のフィルタリング・検索機能の改善
- 請求書コピー機能

### 実装済み（フェーズ4・5・6・7・8）
- `app/admin/invoices/page.tsx` - 請求書一覧ページ ✅
- `app/admin/invoices/new/page.tsx` - 請求書作成ページ ✅
- `app/admin/invoices/[id]/edit/page.tsx` - 請求書編集ページ（PDFダウンロード・発行機能追加済み） ✅
- `app/api/admin/invoices/route.ts` - 請求書CRUD API ✅
- `app/api/admin/invoices/[id]/route.ts` - 請求書個別操作API（明細更新機能追加済み） ✅
- `app/api/admin/invoices/[id]/pdf/route.ts` - PDF生成API ✅
- `app/api/admin/invoices/generate-number/route.ts` - 請求書番号自動生成API ✅
- `app/api/admin/invoices/calculate-details/route.ts` - 請求書明細自動計算API ✅

## ⚠️ 注意事項

1. **サイドバーの表示制御**: 請求書関連メニューは`enableInvoice`設定が`true`の場合のみ表示する必要があります。`components/Sidebar.tsx`を確認して実装してください。

2. **請求書番号の自動採番**: 請求先企業の`invoiceNumberPrefix`を使用して、連番を付与する必要があります。例: `INV-2026-001`

3. **勤怠データの計算**: シフトデータと打刻データを組み合わせて、正確な勤務日数と残業時間を計算する必要があります。

4. **消費税計算**: 請求先企業の`taxRate`を使用して消費税を計算します。デフォルトは10%（0.1）です。

5. **マイグレーション**: `migration_add_invoice_tables.sql`と`migration_add_enable_invoice.sql`は既に実行済みです。

6. **デプロイと動作確認**: 実装後は都度Vercel CLIでデプロイして本番環境で動作確認を行います。
   ```bash
   vercel --prod
   ```
   または
   ```bash
   vercel deploy --prod
   ```

## 📚 参考資料

- `INVOICE_IMPLEMENTATION.md` - 詳細な実装手順書
- `実装状況_詳細.md` - 全体の実装状況
- `IMPLEMENTATION_STATUS.md` - ページ単位の実装状況

## 🚀 デプロイ手順

実装後は以下のコマンドで本番環境にデプロイして動作確認を行います：

```bash
# 本番環境にデプロイ
vercel --prod

# または
vercel deploy --prod
```

デプロイ後、本番環境で以下を確認：
- サイドバーに請求書管理メニューが表示されるか（`enableInvoice`が`true`の場合）
- 請求書一覧ページが正常に表示されるか
- 請求書の作成・編集・削除が正常に動作するか
- フィルタリング機能が正常に動作するか

## 🎯 次のステップ

1. **フェーズ9の実装**: UI/UX改善から開始
2. **フィルタリング・検索機能**: 請求書一覧の検索・フィルタリング機能の改善
3. **請求書コピー機能**: 既存の請求書をコピーして新規作成する機能
4. **バリデーション・エラーハンドリング**: 入力値の検証とエラー処理の強化
5. **デプロイと動作確認**: 実装後は都度Vercel CLIでデプロイして本番環境で確認

---

**最終更新日**: 2026年2月6日  
**実装者**: AI Assistant  
**実装完了**: ✅ 全フェーズ実装完了

## 🎉 実装完了サマリー

請求書機能の全フェーズ（1〜10）の実装が完了しました！

### 実装済み機能一覧
1. ✅ データベーススキーマ設計・実装
2. ✅ 請求先企業マスタ管理機能
3. ✅ 従業員管理への請求情報追加
4. ✅ 請求書管理機能（基本）
5. ✅ 請求書作成フォーム
6. ✅ 勤怠データからの自動計算機能
7. ✅ 請求書編集機能
8. ✅ 請求書PDF生成機能
9. ✅ UI/UX改善（検索・コピー・バリデーション・エラーハンドリング）
10. ✅ テスト・デプロイ（動作確認チェックリスト作成、本番環境デプロイ完了）

### 次のステップ
1. **動作確認**: `請求書機能_動作確認チェックリスト.md`を使用して全機能の動作確認を実施してください
2. **本番環境での確認**: デプロイ済みの本番環境で実際のデータを使用して動作確認を行ってください
3. **ユーザーフィードバック**: 実際のユーザーに使用してもらい、フィードバックを収集してください

### デプロイ情報
- **本番環境URL**: https://rakupochi-kintai-e7syt0jzc-aims-projects-264acc6a.vercel.app
- **デプロイ日**: 2026年2月6日
- **ビルドステータス**: ✅ 成功

不明な点があれば`INVOICE_IMPLEMENTATION.md`を参照してください。
